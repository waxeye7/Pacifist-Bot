# Critic — dest-22 one knob

Default SEND BACK. Cycle-21 watching. Do not seed. Do not push-pacifist.

| pick | knob | flood / future | call |
| --- | --- | --- | --- |
| **A** | dest-cheap `homeMinerBestWork === 0` | leftover 1W still fills; 0-miner HOL-550 still heals | **SHIP src, not dest-21** |
| B | one-shot 5W overlap unshift | fiveWQueued write-only today → must also READ or it floods | SEND BACK this dest |
| C | lastSpawn=0 poke + fiveWQueued READ | cycle-13/14 gun; clamp skip exists but poke is still the gun | SEND BACK |

**Pick A.** Cycle-20 E18S9: dest-cheap `< 2` rewrote HOL 5W while leftover 1W/1+1 lived, then lastSpawn+1500. A stops that. Does not restore poke. leftover-5 still holds. RCL8 leftover ext after storage unchanged.

Src staged `rooms.spawning.ts` dest-cheap `=== 0`. Dest-21 compile stays WORK<2.
