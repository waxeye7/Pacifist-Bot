# Cycle-17 mid-race — **DIRTY**

`run-2026-08-16T04-56-08Z` live binary **changed**. Dest still `pacifist` (`package.json` / `screeps.json`). Control `e839fc8` not pushed.

Seed 04:56 `seed-clean` push = original knob (max **3**, hub-ring prefix, far>12 janitor). Film E12S3: **3 hub-ring** `30,20 30,21 30,22` at 0/300.

Then session `019ff901` ran `npm run push-pacifist` **twice** while watch **38788** was live:

1. Harden: max **8**, 2 builders, janitor **keeps** haul even if >12, roads before source boxes, RCL2-or-3 after slam-5.
2. `walkLine` haul (A* had collapsed to hub ring); ring deferred to RCL4.

Also `push-main` + `push-vps` (empire, not race dest). Ledger `_cycle17-*` still say “did not push” — those notes are stale.

**17 is dirty.** Mean / KEEP / SEND BACK vs this seed are invalid. Wait budget or call CENSOR. Next isolated seed after FINAL.
