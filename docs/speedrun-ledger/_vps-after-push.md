# VPS/main after empire pushes · 2026-08-16T03:33Z

HTTP GET only after `push-vps` + `push-main` (twice). No SSH. No `push-race`.

| | |
| --- | --- |
| `modules.main` sha256 | `554fa2074947fe9d…` both dests |
| Needles | `_roomShellSitesOnly` · `boxMin` · `Cover every spawnless` · `visible foreign spawn, still spawnless` |

W2N1 already closed the shell **before** this push (1 road / 38 ramp / 0 sites / storage 224). Freeze correct. Token gone.
W3N3 still RCL2 · hub container ~200e · 2 builders 0e. `boxMin` 50 is the fix — watch next fire if they tap 28,26.

Official: E36N58 spawn live · E37N57 CB on site · E36N57 KEEP. `claimed` E35N59 should abort on vision or at 82280940.
